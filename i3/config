#i3 config file (v4)

exec_always feh --bg-scale ~/bg/waifu.jpg

set $mod Mod1

bindsys $mod+a exec rofi -show run

font pango:monospace 8

bindsys $mod+Shift+r restart

bar {
  status_command i3blocks
}
